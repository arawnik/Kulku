namespace Kulku.Domain.Network;

/// <summary>
/// Represents the communication channel used for a network interaction.
/// </summary>
public enum InteractionChannel
{
    CvContactForm,
    Email,
    Call,
    LinkedIn,
}
