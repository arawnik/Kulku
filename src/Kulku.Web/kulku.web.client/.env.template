# Public site key for reCAPTCHA
NEXT_PUBLIC_RECAPTCHA_SITE_KEY=your-recaptcha-site-key

# Hostname mappings used by next-intl for hostname → language resolution.
# Comma separated list of hostnames for each language.
NEXT_PUBLIC_EN_HOSTS=en.localhost:3000,en.example.com
NEXT_PUBLIC_FI_HOSTS=fi.localhost:3000,fi.example.com

# API_BASE_URL (server-side).
API_BASE_URL=https://localhost:7219

# Base protocol used when constructing absolute URLs (http or https)
BASE_PROTO=https
